# Notes App

A simple **command-line notes application** built with **JavaScript** and **Node.js**.  
This app allows you to create, read, edit, list, and delete notes directly from the terminal.

---

## Features

- ➕ Add new notes with a title and comment

  - node app.js add --title="..." --comment="..."

- ❌ Remove a single note by title

  - node app.js remove --title="..."

- 🗑 Remove all notes at once

  - node app.js remove-all

- 📋 List all note titles

  - node app.js list

- 📖 Read a note by title

  - node app.js read --title="..."

- ✏️ Edit the comment of an existing note
  - node app.js --title="..." --comment="..Add new.."

---

## Installation

1. Clone the repository
2. Install dependencies (npm install)
3. Run commands in the terminal
